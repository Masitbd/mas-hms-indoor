export type TBeds = {
  worldName: string;
  charge: number;
  fees: number;
  beds: [];
};

export type TAllocation = {
  bedName: string;
  isAllocated: boolean;
  phone: string;
  floor: string;
};
